---
import MainLayout from "../layouts/MainLayout.astro";
import TicketInput from "../components/react/TicketInput.jsx"; // Asegúrate de ajustar la ruta
---

<MainLayout>
  <div class="flex justify-center items-center h-32 bg-gray-100">
    <h1 class="text-3xl font-bold text-gray-800">Bienvenido a reportes CID</h1>
  </div>

  <div class="flex flex-col items-center mt-16 space-y-8 mx-4">
    <div class="flex justify-around w-full max-w-4xl">
      <!-- Ticket Input Island -->
      <div class="bg-white shadow-lg rounded-lg p-8 max-w-sm w-full flex flex-col transition-transform duration-300 hover:scale-105 hover:shadow-xl">
        <TicketInput client:load />
      </div>

      <a href="/report" class="bg-white shadow-lg rounded-lg p-8 max-w-sm w-full flex flex-col items-center text-center no-underline transition-transform duration-300 hover:scale-105 hover:shadow-xl">
        <div class="flex flex-col justify-center h-full">
          <h3 class="text-xl font-semibold mb-4">Registra un nuevo reporte</h3>
          <p class="text-gray-600">
            Al registrar un reporte nuestro equipo adquiere la información necesaria para atender tu problema con mayor solvencia.
          </p>
        </div>
      </a>
    </div>
  </div>
</MainLayout>
